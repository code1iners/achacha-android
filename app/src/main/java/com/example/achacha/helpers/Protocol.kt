package com.example.achacha.helpers

import com.example.achacha.R

object Protocol {

    // note. container
    const val MAIN_CONTENTS_CONTAINER = R.id.mainActivity__body_container

    // note. mode
    const val USER_PROFILE = "USER_PROFILE"
    const val USERNAME = "USERNAME"
}